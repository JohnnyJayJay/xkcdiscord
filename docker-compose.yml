version: "3.9"
services:
  server:
    image: johnnyjayjay/xkcdiscord:latest
    volumes:
      - ./config:/usr/app/xkcdiscord/config:ro
    ports:
      - "8090" # change if you set a different port in your config
